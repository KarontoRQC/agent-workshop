# consoleBranding.ts

## 职责

在前端启动时向浏览器开发者控制台仅输出一次“神兽保佑，代码无 bug”的羊驼字符画，不渲染到页面 UI。

## 关键导出

- `printConsoleBranding`: 只打印羊驼字符画，并保证同一页面生命周期只输出一次。

## 修改指南

- 控制台彩蛋只能保留羊驼字符画，禁止恢复联系方式、搞笑语录、品牌标题或其他前置文案。
- 保持输出仅发生在控制台，不要引入页面可见状态。
